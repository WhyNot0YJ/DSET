model:
  model_name: "yolov8s.pt"
  pretrained_weights: "pretrained/yolov8s.pt"

training:
  epochs: 100
  batch_size: 16
  imgsz: 640
  
data:
  data_yaml: "datasets/DAIR-V2X_YOLO/dairv2x.yaml"

checkpoint:
  log_dir: "logs"

misc:
  device: "cuda"
  num_workers: 2

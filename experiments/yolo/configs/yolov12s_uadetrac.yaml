model:
  model_name: "yolo12s.pt"
  pretrained_weights: "pretrained/yolo12s.pt"

training:
  epochs: 100
  batch_size: 32
  imgsz: 640
  seed: 42
  deterministic: false

data:
  data_yaml: "/root/autodl-fs/datasets/UA-DETRAC_YOLO/data.yaml"

checkpoint:
  log_dir: "logs"

misc:
  device: "cuda"
  num_workers: 2

model:
  model_name: "yolov10n.pt"
  pretrained_weights: "pretrained/yolov10n.pt"

training:
  epochs: 100
  batch_size: 32
  imgsz: 640

data:
  data_yaml: "datasets/UA-DETRAC_YOLO/data.yaml"

checkpoint:
  log_dir: "logs"

misc:
  device: "cuda"
  num_workers: 2

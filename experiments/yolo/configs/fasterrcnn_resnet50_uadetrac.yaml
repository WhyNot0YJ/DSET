model:
  model_name: "fasterrcnn_resnet50_fpn"

training:
  epochs: 100
  batch_size: 8
  imgsz: 640
  seed: 666
  deterministic: false

data:
  data_yaml: "/root/autodl-fs/datasets/UA-DETRAC_YOLO/data.yaml"

checkpoint:
  log_dir: "logs"

misc:
  device: "cuda"
  num_workers: 2

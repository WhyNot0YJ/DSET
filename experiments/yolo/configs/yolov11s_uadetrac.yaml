model:
  model_name: "yolo11s.pt"
  pretrained_weights: "pretrained/yolo11s.pt"

training:
  epochs: 100
  batch_size: 16
  imgsz: 640

data:
  data_yaml: "datasets/UA-DETRAC_YOLO/data.yaml"

checkpoint:
  log_dir: "logs"

misc:
  device: "cuda"
  num_workers: 2

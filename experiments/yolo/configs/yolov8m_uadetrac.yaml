model:
  model_name: "yolov8m.pt"
  pretrained_weights: "pretrained/yolov8m.pt"

training:
  epochs: 100
  batch_size: 32
  imgsz: 640
  seed: 666
  deterministic: false

data:
  data_yaml: "/root/autodl-fs/datasets/UA-DETRAC_YOLO/data.yaml"

checkpoint:
  log_dir: "logs"

misc:
  device: "cuda"
  num_workers: 2

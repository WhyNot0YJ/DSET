---
description: 注释与 docstring 精简，避免跨模块/框架的冗余对比句
globs: "**/*.{py,md}"
alwaysApply: true
---

# 注释与 docstring

- **禁止**在注释或 docstring 里写仅用于「对齐某实现」的套话，例如：`与 RT-DETR 一致`、`与 xxx 相同`、`参照 yyy`。读者维护本仓库即可，不必反复点名其他子项目。
- **应写**：当前函数/文件**实际做什么**、输入输出、关键约定（顺序、默认值、何时跳过）。
- 若必须与另一文件行为绑定，用**可定位的指代**一句即可，例如：`dataset registry 解析方式见 train.py`，避免「一致」类空泛词。
- 英文技术注释（如第三方 vendored 代码）除非本次修改触及，否则不要为风格去批量改。
- **中文用户可见字符串**里不要用括号作补充说明，例如 `(KITTI/Scale)`、`(all)`、`（同 xxx）`。改为逗号或短句：`KITTI 与多尺度`、`全类`、`registry 与 train.py 相同`。
